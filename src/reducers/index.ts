// src/reducers/index.ts
import { combineReducers } from 'redux';

const initialState = {

}

function rootReducer(state = initialState) {
  // Lógica del reductor 1
  return state;
}

export default rootReducer;
